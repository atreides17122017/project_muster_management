import { Component } from '@angular/core';

@Component({
  selector: 'app-login-base', // or whatever your selector is named
  standalone: true,
  imports: [],
  templateUrl: './login.html'
})

export class Login {}
